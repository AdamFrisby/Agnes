<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Agnes.Abstractions\Agnes.Abstractions.csproj" />
  </ItemGroup>

  <!-- Packed at the package root so the host's PluginManifestReader finds it in the .nupkg. -->
  <ItemGroup>
    <None Include="agnes-plugin.json" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
