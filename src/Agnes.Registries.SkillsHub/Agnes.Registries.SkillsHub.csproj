<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Agnes.Abstractions\Agnes.Abstractions.csproj" />
    <!-- SkillsHub indexes GitHub-hosted skills, so it downloads the complete bundle through the shared
         GitHub fetcher rather than re-implementing it (the same relationship the agent plugins have with
         Agnes.Acp). -->
    <ProjectReference Include="..\Agnes.Registries.GitHub\Agnes.Registries.GitHub.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="agnes-plugin.json" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
